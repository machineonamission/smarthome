[Unit]
Description=MinePanel Pod

[Pod]
PodName=minepanel

# Ports are published at the pod level since all containers in the pod
# share one network namespace (this replaces per-container PublishPort=).
PublishPort=8091:8091
PublishPort=25503:3000

# mc-router is optional (see mc-router.container). Uncomment this
# before starting that container, since its port must be reserved here:
#PublishPort=25565:25565

[Install]
WantedBy=default.target
